import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage(`Relic'e Hoşgeldiniz.`)],
  botName: "Müşteri Destek Hattı",
}

export default config
